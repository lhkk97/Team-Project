<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.human.project.iMember">
<select id="memberList" resultType="com.human.project.Member">
	select a.name, a.gender, a.userid, a.passcode, a.mobile, b.type as member_type, a.login_time, a.logout_time 
	from member a, membertype b
	where a.member_type=b.code(+)
</select>
<insert id="insertMember">
	insert into member(name,gender,mobile,userid,passcode)
	values(#{param1},#{param2},#{param3},#{param4},#{param5})
</insert>

<update id="login">
	update member set login_time=to_char(sysdate,'mm/dd hh24:mi:ss')
	where userid=#{param3}
</update>
<update id="logout">
	update member set logout_time=to_char(sysdate,'mm/dd hh24:mi:ss')
	where userid=#{param3}
</update>

<!-- 회원관리 dlg list -->
<select id="dlgList" resultType="com.human.project.Member">
	select a.userid,a.name,a.gender,a.mobile,b.type as member_type
	from member a, membertype b
	where a.member_type=b.code(+)
	and userid=#{param1}
</select>
</mapper>